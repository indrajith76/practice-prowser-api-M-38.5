/* const getInputFieldValueById = (id) => {
      const inputField = document.getElementById(id);
      const inputValue = inputField.value;
      inputField.value = '';
      return inputValue;
}

const addToLocalStorage = () => {
      const name = getInputFieldValueById('name-field');
      const age = getInputFieldValueById('age-field');

      localStorage.setItem(name, age);
}

const removeLocalStorageData = () => {
      localStorage.clear();
} */

/* const name = {
      firstName : 'Abraham',
      LastName : 'Linkon'
}

const nameStrigify = JSON.stringify(name);
localStorage.setItem("name", nameStrigify) */